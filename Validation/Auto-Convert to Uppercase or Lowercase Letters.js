// These are custom validation scripts

// Automatically convert entry to uppercase
event.value = event.value.toUpperCase();

// Automatically convert entry to lowercase
event.value = event.value.toLowerCase();
